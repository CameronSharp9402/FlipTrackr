<Window x:Class="CSharpResaleBusinessTracker.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="300" Width="300">

    <Window.Resources>
        
        <!-- Fonts -->
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <!-- Hover Animations -->
        <Style x:Key="HoverButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FFDDDDDD"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Padding" Value="6,2"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="6" Background="{TemplateBinding Background}" >
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFBEE6FD"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFC4E5F6"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#FFC0C0C0"/>
                                <Setter Property="Foreground" Value="#FF707070"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Section Container Style -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="2" Opacity="0.2"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Fade-in on load -->
        <Style TargetType="Grid" x:Key="FadeInGrid">
            <Style.Triggers>
                <EventTrigger RoutedEvent="Grid.Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                      From="0" To="1"
                                      Duration="0:0:0.5"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <StackPanel Background="{DynamicResource BackgroundBrush}">
        <Separator Margin="0 0 0 25" />
        <TextBlock Text="ROI Threshold (%)" Margin="0 0 0 10" HorizontalAlignment="Center" Foreground="{DynamicResource ForegroundBrush}"/>
        <TextBox x:Name="RoiThreshold" Width="100" Margin="0 0 0 10"/>
        <Separator Margin="10"/>
        <TextBlock Text="Select Currency:" HorizontalAlignment="Center" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}"/>
        <ComboBox x:Name="CurrencyComboBox" Width="200" Margin="10" SelectionChanged="CurrencyComboBox_SelectionChanged">
            <ComboBoxItem Content="USD" Tag="en-US" />
            <ComboBoxItem Content="EUR" Tag="fr-FR" />
            <ComboBoxItem Content="GBP" Tag="en-GB" />
            <ComboBoxItem Content="JPY" Tag="ja-JP" />
            <ComboBoxItem Content="CAD" Tag="en-CA" />
            <ComboBoxItem Content="AUD" Tag="en-AU" />
        </ComboBox>
        <Separator Margin="10"/>
        <Button Content="Save Changes" Click="SaveButton_Click" Style="{StaticResource HoverButtonStyle}"/>
        <Button Content="Toggle Dark Mode" Click="ToggleDarkMode_Click" Width="150" Margin="10" Style="{StaticResource HoverButtonStyle}"/>
    </StackPanel>
</Window>
